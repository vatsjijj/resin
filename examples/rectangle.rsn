class Rectangle {
  func init(rectWidth, rectHeight) {
    this.width = rectWidth
    this.height = rectHeight
  }

  func printArea() {
    let area = this.width * this.height
    println(area)
  }

  func setHeight(newHeight) {
    this.height = newHeight
  }

  func setWidth(newWidth) {
    this.width = newWidth
  }
}

let box = Rectangle(10, 5)
box.printArea()
box.setHeight(3)
box.printArea()